%h2 Events
- @events.each do |event|
  .event
    %h3= event.name
    %p= event.datetime
    %p= event.timezone
    %p= event.latitude
    %p= event.longitude
    %p= event.message

%p= link_to 'New event', new_event_path
