%h2 Events

%p= link_to 'New event', new_event_path, :class => "btn small success"

- @events.each do |event|
  .event
    %h3= event.name
    %p= event.datetime.strftime("%B %d, %Y at %I:%M %p")
    %p= event.timezone
    %p= event.message
